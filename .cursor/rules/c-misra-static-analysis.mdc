---
description: 生成/修改 C 代码时尽量满足 MISRA，便于 IAR C-STAT 通过
globs:
  - "**/*.c"
  - "**/*.h"
alwaysApply: false
---

# MISRA 取向（Agent 生成代码的目标）

**本规则的目的**：在编写或修改 **`.c` / `.h`** 时，让你产出的代码**尽量符合 MISRA C**（工程里通常按 **MISRA C:2012** 或 **MISRA C:2023** 启用规则），以便用户在 **IAR EWARM** 里用 **C-STAT** 检查时**少违规、少返工**。  
**验证方式**以 **IAR C-STAT** 为准；本规则是写作约束，**不能**代替在 IAR 里实际跑一遍 C-STAT。

## 与其它仓库规则的关系

必须先遵守 **`c-naming-conventions.mdc`**、**`c-brace-layout.mdc`**、**`c-doc-comments.mdc`**（花括号、命名、注释本身也常与 MISRA 可读性/可维护性要求一致）。

## 生成代码时的取向（面向 C-STAT / MISRA）

下列面向**常见** MISRA 类问题，**非**条款逐条对应；若与某条 HAL/SDK 或团队偏差冲突，**以用户工程与 C-STAT 配置为准**。

- **控制流**：`if` / `else` / `while` / `for` / `switch` 使用复合语句时**左 `{` 独占一行**（见花括号规则）；避免空 `if` 体仅挂一个分号等易误判写法。
- **类型与转换**：减少隐式整型混算；必要处**显式转换**并写明意图；注意**有符号/无符号**比较与循环上界（避免典型越界、死循环模式）。
- **宏**：参数加括号、整体必要时加括号，避免带副作用实参被多次求值类写法；新代码避免「难以静态分析的复杂宏」。
- **指针与数组**：不编造越界访问；`memcpy`/`memset` 等长度与对象大小一致；注意 `const`、指针为空时的行为在注释或契约里写清。
- **返回值与副作用**：关注可能失败 API 的返回值（或按项目惯例显式忽略并注释）；避免未使用关键返回值。
- **标准库**：新代码优先使用项目已有、**已知在 MISRA 策略下允许**的接口；避免在未约定时引入高风险接口（例如未限长的字符串操作等）；**不要**为「省事」引入与团队 C-STAT 配置冲突的用法。
- **注释与可审查性**：对外 API、关键假设、非显而易见处写清（见注释规则），便于人工与 C-STAT **偏差**说明。

## 验收与冲突处理

- **用户侧**：在 IAR 中对改动跑 **C-STAT**；若告警来自 ST HAL 等第三方，按项目惯例处理（抑制、封装或偏差），**不要**为消除告警大面积破坏可读结构。
- **Agent 侧**：若用户说明「仅 HAL 区/必须保留某写法」，在应用层新代码中仍坚持上述取向；**若与 C-STAT 冲突，以用户指定为准**。

## 工具（辅助，非验收标准）

| 用途 | 说明 |
|------|------|
| **IAR C-STAT** | **MISRA 条款级验收**以工程内启用的规则集为准。 |
| **`.clang-tidy`** | 仓库根目录；编辑期辅助（`bugprone-*`、`cert-*-c`、花括号等），**不替代 C-STAT**。 |
| **`compile_commands.json`** | **`iar_to_compile_commands.py`** 从 **`.ewp`** 生成，供 clangd/可选 clang-tidy；**不替代 IAR 编译**。 |

仅在本地用 **clang-tidy** 分析且需解析 `<math.h>` 等时，才可能要为生成脚本配置 **GNU Arm** 工具链根目录（`--gcc-toolchain` / 环境变量）；**只做 IAR + C-STAT 时可忽略**。

## 命令行（可选）

```bash
clang-tidy -p . path/to/file.c
```

若 `cert-*` 等在某版 LLVM 中不可用，可从 **`.clang-tidy`** 中删除对应检查项。
