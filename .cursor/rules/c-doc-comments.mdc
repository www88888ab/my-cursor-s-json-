---
description: C 源码注释规范与自动生成要求（Doxygen/块注释，中文说明）
globs:
  - "**/*.c"
  - "**/*.h"
alwaysApply: false
---

# C 注释规范（与命名规范配合）

在编写或修改 `.c` / `.h` 时，**必须为对外符号补全或更新注释**。推荐模板化风格如下，便于跨工程复用本规则：

- **语言**：说明性文字以**中文**为主（标识符、类型名、官方 API 名保持源码原样）。
- **形式**：对外 API 使用**块注释**，关键接口配合 **Doxygen 风格标签**（`@brief`、`@param`、`@return`、`@note` 等）；与仅含 `@Description` / `@param` 的旧式块注释可二选一，同一符号勿混用两套标签体系。
- **语气**：写清「做什么、参数与返回值含义、失败或 `NULL` 行为、副作用」，避免复述函数名式的空话。

## 必须写注释的场景

- 新建或导出的 **`extern` 函数**、**全局变量**（`g_`）、**对外 `typedef`/结构体/枚举**（在头文件中）。
- **`static` 函数**：若逻辑非一眼可读（算法、状态机、协议、硬件时序），必须简要说明职责与关键约定。
- 修改函数**签名、返回值语义、副作用**（写 Flash、改全局状态）时，**同步更新**注释。

## 头文件：类型、宏与全局变量（改写/重命名时一并补齐）

**改写、重命名或拆分模块时，不得只改标识符而不维护注释**；下列项在头文件中**缺注释视为未完成**。

### 结构体 / 联合体

- 在 `typedef struct`（或 tag）上方用 `/** @brief … */` 说明**整体语义**（存储什么、与谁同步更新）。
- **每一个成员**必须有自己的中文说明，推荐行尾 Doxygen：`int16_t accelXRaw; /**< … */`
- 注释需写清**物理含义**；若为测量量，写明**单位**（如 m/s?、°/s、℃、ms）；指针写明**所有权/生命周期**或**仅输出**。

### 枚举

- 枚举类型上方 `@brief` 说明用途。
- **每个枚举常量**加行尾 `/**< … */` 或等价简短中文，说明该状态/取值的业务含义（勿只写英文标识符复述）。

### `#define` 与对外常量

- **非自解释的**配置宏、超时、阈值、缓冲区大小、状态码、寄存器地址块等：在宏**上一行**写 `/** … */`，或行尾 `/**< … */`。
- **成组寄存器地址**：可在组首写一个总述注释（如「与数据手册寄存器表一致」），对**业务相关**或**易混**的个别宏仍单独说明。
- 仅当宏名与注释会完全重复且无附加信息时，才可省略（尽量少用）。

### `extern` 全局变量

- **每个**对外全局（含 `g_`）单独用 `/** … */` 说明：含义、**由谁更新**（哪个函数/周期任务）、**无效或复位时的语义**（若适用）。
- 与通信/显示绑定的缓冲区，可注明协议或消费方。

### 实现文件中的静态数据（`.c`）

- 模块内 **`static` 文件作用域变量**若表示滤波器状态、状态机、缓存等，在声明处用**分组注释**或**逐行**说明物理意义；避免大段无说明的 `static` 数组/计数器。

## 推荐格式

### 头文件 / 实现文件顶部（可选但新文件建议有）

```c
/**
 * @file    foo.c
 * @brief   一句话说明本文件职责
 * @details 可选：与其它模块关系、线程/中断上下文约束
 */
```

### 对外函数（头文件声明处优先写全；实现处可写 `@brief` + 细节或见声明）

```c
/**
 * @brief  一句话说明做什么。
 * @param  paramName 参数含义与单位/范围；指针写明方向（入参/出参）。
 * @return 返回值含义；`void` 可写「无」或省略 `@return`。
 * @note   可选：调用上下文、重入性、与硬件时序相关约束。
 */
void Module_DoWork(uint16_t index, uint8_t *outBuf);
```

与历史代码混排时，允许使用等价块（二选一，勿混用同一函数两种风格）：

```c
/***************************************************************************
 * @Description: 一句话说明
 * @param: index - 说明
 * @param: outBuf - 说明
 * @return: 说明
 * @fixme: 无
 **************************************************************************/
```

### 宏 / 枚举 / 结构体（与上一节「头文件」要求一致）

- 对象式宏、枚举取值：**禁止**只写「见手册」而无语义；须说明在本工程中的用途或约束。
- 结构体：**每个成员**行尾注释（物理含义与单位），与 §「头文件：类型、宏与全局变量」相同。

## 自动生成行为（Agent）

- **新增 API**：先写注释块，再写声明/定义；参数名与 `@param` 一致。
- **重构/重命名**（含 `Mpu6050_` 这类模块前缀调整）：对涉及的 **struct/enum/宏/`extern`** 按本节 **「头文件：类型、宏与全局变量」** 逐项检查，**漏注释则补全**，过时注释则改正。
- **禁止**生成无意义注释（如「初始化函数」重复函数名）；应说明**副作用、约束、失败行为**。
- 英文 HAL/SDK 符号（如 `HAL_I2C_Mem_Read`）不要强行包一层中文包装注释，仅在调用处必要时用 `@note` 说明为何如此调用。

## 与命名规范的关系

若项目同时配置了命名约定规则，注释里的 `@param` 等应使用**与声明一致的标识符**（如参数为 `lowerCamelCase` 则注释中写同名，勿替换为另一种风格）。

## 花括号布局

控制流与函数体的 `{` `}` 换行规则见 **`c-brace-layout.mdc`**（与注释规则独立，编写代码时一并遵守）。
