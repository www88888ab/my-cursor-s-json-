# oh-my-zsh 手动安装和配置指南

## 📋 在 zsh 中手动安装 oh-my-zsh

由于自动安装遇到网络问题，请按照以下步骤手动安装：

### 步骤 1: 切换到 zsh

在 Cursor 的 MSYS2 终端中运行：
```bash
zsh
```

### 步骤 2: 安装 oh-my-zsh

在 zsh 中运行：
```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

如果网络问题，可以：
1. 多次重试
2. 或使用代理
3. 或手动下载安装脚本

### 步骤 3: 安装插件

安装完成后，在 zsh 中运行：

```bash
# 进入插件目录
cd ~/.oh-my-zsh/custom/plugins

# 安装语法高亮插件
git clone https://github.com/zsh-users/zsh-syntax-highlighting.git

# 安装自动补全插件
git clone https://github.com/zsh-users/zsh-autosuggestions.git

# 安装增强补全插件
git clone https://github.com/zsh-users/zsh-completions.git
```

### 步骤 4: 配置 .zshrc

`.zshrc` 文件已经配置好了，包含：
- ✅ Windows Git 和 Python 配置
- ✅ 中文支持
- ✅ 插件配置
- ✅ 自定义提示符

### 步骤 5: 重新加载配置

```bash
source ~/.zshrc
```

## 🎯 验证安装

安装完成后，运行以下命令验证：

```bash
# 检查 oh-my-zsh
echo $ZSH

# 检查插件
ls ~/.oh-my-zsh/custom/plugins/

# 测试功能
# 1. 输入错误命令应该显示红色（语法高亮）
xyz123

# 2. 输入之前用过的命令，应该显示灰色建议（自动补全）
# 按右箭头键接受建议

# 3. 按 Tab 键应该有增强的补全功能
```

## 🔧 如果遇到网络问题

### 方法 1: 使用镜像源

```bash
# 使用 gitee 镜像（如果可用）
git clone https://gitee.com/mirrors/oh-my-zsh.git ~/.oh-my-zsh
```

### 方法 2: 手动下载

1. 访问: https://github.com/ohmyzsh/ohmyzsh/archive/refs/heads/master.zip
2. 下载并解压到 `~/.oh-my-zsh`
3. 运行安装脚本的配置部分

## ✅ 配置清单

安装完成后，应该有以下内容：

- [ ] oh-my-zsh 已安装 (`~/.oh-my-zsh` 目录存在)
- [ ] zsh-syntax-highlighting 插件已安装
- [ ] zsh-autosuggestions 插件已安装
- [ ] zsh-completions 插件已安装
- [ ] `.zshrc` 已配置
- [ ] 运行 `source ~/.zshrc` 无错误
- [ ] 提示符显示正常
- [ ] 语法高亮工作正常
- [ ] 自动补全建议工作正常

## 🎉 完成

配置完成后，您就可以享受强大的 zsh 命令行体验了！
